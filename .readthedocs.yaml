version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"

sphinx:
  configuration: docs/conf.py
  builder: html

formats:
  - pdf  

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
